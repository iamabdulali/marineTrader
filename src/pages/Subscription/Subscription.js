import React from "react";
import Layout from "../../components/Layout/Layout";

const Subscription = () => {
  return (
    <Layout>
      <p>Subscription</p>
    </Layout>
  );
};

export default Subscription;
